# API_KEY=choose-a-secret-here  # optional — if unset, auth is disabled
YOOBI_BASE_URL=https://yourcompany.yoobi.nl/
YOOBI_USERNAME=your-username
YOOBI_PASSWORD=your-password

# READONLY=true              # disable write endpoints (POST /api/hours, POST /api/week/close)

# Deployment (optional, used by make deploy)
# HOST=root@your-server
